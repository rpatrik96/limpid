{
  "name": "@coach/providers",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Host-side LanguageModel adapters (OpenAI-compatible, Anthropic, Claude Code CLI) shared by the extension and the eval. Uses fetch/child_process; not one of the pure core packages.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@coach/contract": "*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.6.3"
  }
}
