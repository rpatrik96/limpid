# Ship only the bundled output + metadata. Everything is bundled into
# dist/extension.cjs by esbuild, so source, types, and node_modules are excluded.
.vscode/**
src/**
node_modules/**
**/*.ts
**/*.map
tsconfig.json
esbuild.config.mjs
.gitignore
