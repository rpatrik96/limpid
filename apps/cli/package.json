{
  "name": "@coach/cli",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "limpid CLI — deterministic writing checks for CI / pre-commit gates on .tex / .md (and prose).",
  "bin": {
    "limpid": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@coach/coach": "*",
    "@coach/contract": "*",
    "@coach/engine": "*",
    "@coach/latex": "*",
    "@coach/markdown": "*",
    "@coach/rubric": "*"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3"
  }
}
